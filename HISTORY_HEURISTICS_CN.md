# 進階搜尋歷史啟發技術說明

本文檔說明本引擎引入的兩項關鍵搜尋增強技術：**Continuation History（延續歷史）** 與 **Correction History（修正歷史）**。這些技術源自 Stockfish 等頂尖引擎的開發經驗，旨在提升移動排序的效率與靜態評估的準確性。

## 1. Continuation History (延續歷史)

### 概念
傳統的歷史啟發（History Heuristic）僅追蹤「某個移動在任意局面下的成功率」。然而，西洋棋的移動往往具有連貫性與對話性。Continuation History 試圖捕捉「基於上一手棋的脈絡，當前移動的好壞」。

簡單來說，它回答了這個問題：**「當對手（或我方）走了這一步後，這一步回應通常是好棋嗎？」**

這能捕捉到棋子間的協作關係與戰術組合，例如：
*   如果上一手是「馬跳到 F3」，那麼「象走到 G5」可能是一個強力的後續手段。
*   如果上一手是對手「兵推到 E4」，那麼我方「馬跳到 F6」可能是一個好的反擊。

### 實作方式
我們建立了一個 **4D 資料結構** 來存儲這些統計數據：
`continuation_history[prev_piece][prev_to][curr_piece][curr_to]`

*   **索引**：
    *   `prev_piece`, `prev_to`: 前一手棋的棋子類型與目標格。
    *   `curr_piece`, `curr_to`: 當前考慮移動的棋子類型與目標格。
*   **更新機制**：
    *   當一個寧靜步（Quiet Move）引發截斷（Beta Cutoff）時，視為成功，增加分數。
    *   當一個寧靜步被搜尋但未能引發截斷（搜尋失敗）時，視為失敗，扣除分數。
*   **應用**：
    *   在 `score_moves` 階段，我們查表獲取分數並加到移動的排序分數中。這使得那些在歷史上「作為特定前手棋的回應」表現良好的移動，會被優先搜尋。

### 對引擎強度的提升
*   **優化移動排序**：透過更精確的上下文感知，引擎能更早找到最佳移動（Best Move）。
*   **加速搜尋**：更早的截斷意味著搜尋樹的規模變小，引擎能在相同的時間內搜尋更深。

---

## 2. Correction History (修正歷史)

### 概念
靜態評估函數（Static Evaluation）通常是基於一組固定的特徵（如材質、PST、兵形等）計算出來的。然而，靜態評估難免存在**系統性偏差**。例如，評估函數可能長期低估某種特定的封閉兵形結構，或者高估某種看起很美但實際無用的空間優勢。

Correction History 的核心思想是：**「如果搜尋結果（動態分數）經常與靜態評估（靜態分數）有巨大落差，我們應該記住這個誤差，並在下次遇到類似局面時主動修正。」**

### 實作方式
我們利用 **Pawn Key (兵形雜湊值)** 來識別局面結構，因為兵形結構通常決定了局面的長期性質。

*   **Pawn Key**：一個 64 位元的 Zobrist Hash，僅由盤面上的兵（位置與顏色）計算得出。
*   **修正表**：`pawn_correction_history` 陣列，以 Pawn Key 為索引。
*   **更新機制**：
    *   當搜尋結束或存入置換表（TT）時，我們計算 `Diff = 搜尋分數 - 原始靜態分數`。
    *   我們使用「重力公式（Gravity Formula）」將這個 `Diff` 緩慢地更新到修正表中。
*   **應用**：
    *   在搜尋開始前，我們根據當前的 Pawn Key 查表，獲取 `Correction` 值。
    *   `調整後的靜態分數 = 原始靜態分數 + Correction`。
    *   這個調整後的分數被用於後續的剪枝決策（如 Null Move Pruning, Futility Pruning）。

### 對引擎強度的提升
*   **減少評估盲點**：引擎能夠「學習」自己評估函數的弱點。如果它發現某類兵形總是比評估的更好，它會自動給予加分。
*   **更穩定的剪枝**：許多剪枝技術依賴靜態評估。如果靜態評估不準，可能會錯誤地剪掉好棋。修正後的評估值能讓剪枝決策更安全、更準確。

---

## 3. 其他相關技術：Butterfly History (蝴蝶歷史啟發)

### 概念
**Butterfly History (蝴蝶歷史啟發)** 是一種特殊的歷史啟發資料結構，其名稱源於資料在 2D 陣列中的分布圖形類似蝴蝶的翅膀（因為合法的移動通常分布在主對角線兩側的特定區域）。

傳統歷史表通常使用 `[piece_type][to_square]` (12 x 64) 的結構，這被稱為 "Piece-To History"。
而 Butterfly History 則使用 **`[from_square][to_square]`** (64 x 64 = 4096) 的結構。

### 特點與差異
*   **精確度**：Butterfly History 比 Piece-To History 更精確，因為它區分了「從哪裡移動過來」。例如，同樣是騎士跳到 F3，從 G1 跳過來（標準開局）和從 D2 跳過來（可能是撤退或調整）在 Butterfly 表中是不同的條目，而在 Piece-To 表中則被混為一談。
*   **空間佔用**：Butterfly 表需要 4096 個 entries，而 Piece-To 表只需要 768 個。雖然較大，但對於現代電腦來說完全可以接受。
*   **本引擎現況**：目前本引擎主要使用 **Piece-To History** 作為基礎歷史啟發，並輔以 **Continuation History** 來捕捉更深層的移動關聯。未來可以考慮引入 Butterfly History 來進一步細化單步移動的歷史統計。

---

## 4. 常見問題 (Q&A)

### Q: 啟用這些功能後，總搜尋節點數 (Total Nodes Searched) 增加，搜尋時間變長，這正常嗎？

**A: 是的，這是預期內的現象，通常代表引擎搜尋得更「深」或更「仔細」。**

1.  **Correction History 減少了「盲目」剪枝**：
    *   許多積極的剪枝技術（如 Futility Pruning, Null Move Pruning）依賴靜態評估值。如果原始靜態評估過低（例如低估了某個攻擊型兵形），引擎可能會過早剪枝，導致漏算關鍵變化。
    *   Correction History 會提高這些局面的評估值，使得引擎**不敢輕易剪枝**，必須搜尋更多節點來確認安全性。
    *   **結果**：搜尋節點增加，但避免了因誤判而丟失好棋（Blunder）。這是一種「有價值的」節點增加。

2.  **Continuation History 的計算開銷**：
    *   雖然 Continuation History 旨在優化排序（減少節點），但維護 4D 表格和計算索引本身需要微小的 CPU 時間。
    *   這可能會導致每秒搜尋節點數 (NPS) 略微下降，使得相同深度的總耗時增加。

3.  **整體效益**：
    *   雖然單次搜尋的節點數可能增加，但由於評估更準確、排序更優，引擎在複雜局面下能找到更強的走法。
    *   搜尋時間的增加是為了換取更高的**勝率**與**準確度**。

---

## 總結

| 技術 | 關注點 | 作用 | 核心機制 |
| :--- | :--- | :--- | :--- |
| **Continuation History** | **移動排序** | 讓好棋更早被搜尋到 | 4D 歷史表 (前手棋 -> 當前棋) |
| **Correction History** | **評估準確性** | 修正靜態評估的系統性誤差 | 基於兵形 Hash 的動態分數修正 |

這兩項技術相輔相成：Continuation History 讓引擎搜得**更快**，而 Correction History 讓引擎搜得**更準**。它們共同構成了現代強大西洋棋引擎的重要基石。
