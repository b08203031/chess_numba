# NNUE V2 架構說明文件 (Stockfish 18 啟發版)

本文檔詳細說明了目前 `chess_engine_v2` 所採用的神經網路評價函數（NNUE）架構及其設計理念。

---

## 1. 架構總覽 (Architecture Overview)

目前的網路是一個四層的前饋神經網路，專門為西洋棋的稀疏特徵與即時推論優化。

**拓撲結構：**
`45056 (Sparse) -> 512 (Accumulator) x 2 -> 1024 -> 128 (Dual Activation) -> 256 -> 64 -> 1 (Logit)`

### 第一層：特徵累積層 (Accumulator / Feature Transformer)
*   **輸入特徵**：HalfKA (King-Relative Piece Square)。考慮了己方國王位置與全場其餘棋子位置的組合，共 **45,056** 種特徵。
*   **維度**：**512**。相較於初版的 256，V2 版本將特徵記憶空間翻倍，能捕捉更細微的局面差異。
*   **計算方式**：採用增量更新（Incremental Update），每走一步只需對 Accumulator 進行加減法運算，複雜度為 O(1)。
*   **激活函數**：`ClippedReLU(0, 127.0)`。

### 第二層：特徵融合層 (Concatenated Logic)
*   **輸入維度**：**1024** (將行棋方 STM 的 512 與非行棋方 NSTM 的 512 向量拼接)。
*   **權重矩陣**：1024 -> 128。

### 第三層：雙路激活層 (Dual Activation Path - SF18 Tech)
這是本架構的核心黑科技，模仿了 Stockfish 18 (SFNNv10) 的設計：
*   **分流運算**：FC2 的 128 個輸出會同時進入兩條路徑：
    1.  **線性路徑 (CReLU)**：保留基礎的局面數值，維持評估穩定性。
    2.  **二次方路徑 (SqrCReLU)**：計算 `(clamp(x, 0, 127)^2) / 127.0`。這能放大強烈的戰術訊號（如強大的牽制或殺招威脅），讓引擎對戰術轉折極度敏感。
*   **合併回流**：將兩路（128 + 128）拼接成 **256** 維向量送入下一層。

### 第四層：決策層 (Output Layers)
*   **FC3**：256 -> 64。進一步壓縮特徵。
*   **FC4**：64 -> 1。最後輸出一個數值（Logit），隨後轉換為 Centipawn (cp) 分數。

---

## 2. 設計理念 (Design Philosophy)

### A. 解決「小腦袋」問題 (Underfitting)
在使用 4,000 萬筆數據進行訓練時，舊版的 256 維 Accumulator 與 32 個神經元邏輯層顯得捉襟見肘（Loss 無法下降）。V2 版透過**拓寬累積層**與**加深邏輯層**，將邏輯參數提升了約 **15 倍**，確保模型有足夠的「容量」去記住大數據中的精細規律。

### B. 戰術與戰略的兼顧
傳統 NNUE 有時在動態戰術（計算）上不如手寫評估函數靈敏。透過引入 **SqrCReLU (平方激發)**，我們人為地在數學上創造了「張量張力」，這讓引擎在面對棄子進攻、閃擊等局面時，能給出比線性模型更果斷的評估。

### C. 零記憶體開銷的工程優化 (Zero-Copy Implementation)
在訓練端，我們實作了手動索引映射（Manual Index Mapping），避免了在 16GB RAM 下因複製 10GB 陣列而導致的崩潰。在推論端（Numba），我們使用預分配的 Stack 緩衝區，確保每一秒能進行數萬次的高速神經網路推論。

---

## 3. 性能指標參考

*   **參數總數**：約 2,300 萬。
*   **訓練目標**：BCEWithLogitsLoss (WDL 機率預測)。
*   **適用資料量**：3,000 萬 ~ 1 億筆盤面。

---
