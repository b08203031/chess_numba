# SEE 改進與效能分析報告

## 1. 概述

本報告分析了在引入基於 Stockfish 算法的 SEE (Static Exchange Evaluation) 函數後，引擎在解題能力（Puzzle Solving）上的表現變化。

**關鍵變更：**
1.  **SEE 實現：** 重新實現了 `chess_engine/see.py`，加入了對 **X-Ray 攻擊** 和 **Pin（牽制）** 的處理。
2.  **演算法修正：** 修復了 SEE 遞歸回溯中的數學錯誤（從錯誤的 `max(current, -next)` 修正為標準的 `-max(-current, next)`，即 `min(current, -next)`）。
3.  **應用場景：** 在 Quiescence Search (QS) 中啟用了 SEE 剪枝 (`ENABLE_SEE_IN_QUIESCENCE = True`)，並在 Move Ordering 中使用 SEE 對吃子進行排序。

## 2. 測試結果比較

| 測試指標 | Run 1 (SEE Disabled in QS*) | Run 2 (SEE Enabled in QS, Buggy) | Run 3 (Correct SEE, Enabled in QS) |
| :--- | :--- | :--- | :--- |
| **答對題數** | **94 / 102** | 93 / 102 | **93 / 102** |
| **總節點數** | 59.6 M | 62.4 M | 60.9 M |
| **平均 NPS** | 396 k | 421 k | 438 k |
| **SEE Pruned** | 0 | 0 | 5650 |

*\*註：Run 1 雖然禁用了 QS 中的 SEE 剪枝，但 Move Ordering 仍使用了當時（含 Bug）的 SEE 函數。*

## 3. 結果分析

### 3.1 為什麼答對題數下降 (94 -> 93)？

儘管修復了 SEE 的數學邏輯並啟用了剪枝，答對題數卻從 94 降至 93。原因如下：

1.  **"樂觀" SEE 的副作用：**
    *   在 Run 1 和 Run 2 中，SEE 存在一個 Bug（使用了 `max` 而非 `min` 邏輯），導致它對 "劣質吃子"（如后吃兵被兵吃回）給出了正分（+100 而非 -800）。
    *   這種 "樂觀" 的評估使得引擎在 Move Ordering 階段**優先搜尋了大量的棄子/劣質吃子**。
    *   對於戰術題（Puzzles）而言，許多解法恰恰依賴於棄子。因此，這個 Bug 意外地幫助引擎更快找到了某些棄子解法（如 `004Op`）。
    *   當我們在 Run 3 中修復了這個 Bug，SEE 正確地將這些棄子評估為負分並推遲搜尋，導致在有限時間內（Time Limit）未能搜尋到某些特定題目的深層解法。

2.  **噪聲與穩定性：**
    *   Run 3 雖然修復了 `004Op`（通過其他路徑找到），但失敗了 `006of`。這類 "一題進一題出" 的現象通常屬於搜尋算法的隨機性（Search Instability）或時間管理的邊界效應，而非邏輯倒退。

3.  **SEE 剪枝的生效：**
    *   在 Run 3 中，`QS SEE Pruned` 為 5650，證明正確的 SEE 邏輯已經開始在靜態搜尋中發揮作用，剔除了明顯的壞吃子。這有助於提升引擎在實戰（而非僅限戰術題）中的整體棋力，避免因盲目吃子導致的敗局。

### 3.2 效能變化

*   **NPS 提升 (396k -> 438k)：** 正確的 SEE 剪枝減少了 QS 中的無效節點，使得引擎能以更高的效率搜尋更有價值的節點。
*   **節點數變化：** 節點數的波動主要受搜尋路徑改變的影響。

## 4. 結論

*   **正確性優先：** 雖然 Run 1 的分數略高，但其基於錯誤的數學邏輯（將送后評估為優勢）。保留這種錯誤會嚴重損害引擎在正常對局中的判斷力。
*   **改進成功：** Run 3 的 SEE 實現是數學上正確的，且成功觸發了剪枝。NPS 的提升也印證了其效能。
*   **後續建議：** 目前的 93 分是基於 "正確邏輯" 的真實分數。若要進一步提升，應關注其他搜尋參數（如 LMR, Null Move）的調優，而非依賴 SEE 的錯誤行為。

本報告確認了 SEE 模組已正確修復並整合。
