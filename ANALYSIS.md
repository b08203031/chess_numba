# 評估函數 `evaluation.py` 全面分析報告

## 引言

本報告旨在深入分析 `evaluation.py` 模組中的所有評估項。分析過程結合了程式碼邏輯審查和使用特定 FEN 棋盤局面進行的單元測試。總體結論是，該評估函數設計精良，邏輯嚴謹，並且在測試中表現出高度的準確性。在分析過程中發現的所有預期與結果不符之處，最終都被證實是手動分析的偏差。然而，分析過程也揭示了**一個潛在的邏輯錯誤**，本報告將對此進行詳細說明。

---

## 核心框架：錐形評估 (Tapered Evaluation)

在分析具體評估項之前，必須先理解整個評估函數的核心框架——錐形評估。引擎不會只用一組靜態的評估值，而是會分別計算一個局面在中局（MG）和殘局（EG）的分數，然後根據目前的遊戲階段值，在這兩個分數之間進行加權平均。

### 1. 遊戲階段 (Game Phase)

*   **作用**: 量化棋局進程，判斷當前是趨近於中局還是殘局。
*   **實現方式**: 根據棋盤上除了兵和王以外的所有棋子的「階段權重」（馬/象=1, 車=2, 后=4）相加得到一個總分 `phase`。`phase` 的最大值為 24（開局狀態）。
*   **實踐邏輯**: 最終分數是透過線性插值公式 `(mg_score * phase + eg_score * (MAX_PHASE - phase)) // MAX_PHASE` 計算得出，使得評估值能隨著棋局進展平滑地過濾。
*   **正確性**: **正確**。這是計算遊戲階段和實現錐形評估的標準方法。

---

## 基礎評估項

### 2. 子力價值 (Material Values)

*   **作用**: 為每種棋子賦予一個基礎分值，是評估函數最根本的組成部分。
*   **實現方式**: 定義了 `MG_MATERIAL_VALUES`（中局）和 `EG_MATERIAL_VALUES`（殘局）兩套價值體系。例如，兵在殘局中的價值更高（120 vs 100），反映了其升變潛力。
*   **正確性**: **正確**。將 MG 和 EG 價值分開計算，並在最後進行插值的邏輯是正確的，數值設定也符合主流引擎的標準。

### 3. 子力方格表 (Piece-Square Tables, PSTs)

*   **作用**: 量化棋子的位置價值。一個位於中心的騎士遠比在角落的騎士更有價值。
*   **實現方式**: 為每種棋子在每個遊戲階段（MG/EG）都定義了一張 64 格的位置價值表。程式遍歷所有棋子，將其位置對應的分數累加到總分上。黑方棋子的位置會被垂直翻轉（`sq ^ 56`）後再查表，這是一個高效的對稱實現。
*   **實踐邏輯**: PSTs 的值設定清晰地反映了戰略原則。例如，`KING_PST_MG` 獎勵易位到角落的王，而 `KING_PST_EG` 則強烈鼓勵王移動到中心。
*   **正確性**: **正確**。這是應用 PSTs 的標準且高效的方法，數值設定符合通用原則。

---

## 結構與佈局評估項

### 4. 兵形結構 (Pawn Structure)

此評估項在 `evaluate_pawn_structure` 函數中實現。
*   **疊兵 (Doubled Pawns)**: 懲罰在同一檔案上出現多於一個的兵。實現方式是遍歷檔案，對數量 > 1 的兵施加懲罰。
*   **孤兵 (Isolated Pawns)**: 懲罰在相鄰檔案都沒有友方兵的兵。實現方式是使用 `ADJACENT_FILES_MASKS` 進行高效檢查。
*   **通路兵 (Passed Pawns)**: 重獎前進道路上沒有敵兵阻擋的兵。獎勵與兵所在的兵線（rank）直接相關，越接近底線獎勵越高。實現方式是使用預先計算的 `PASSED_PAWN_MASKS` 進行高效判斷。
*   **正確性**: **正確**。經過詳細測試，所有兵形結構的評估邏輯都被證實是準確無誤的。

### 5. 棋子佈局與協同 (Piece Placement and Synergy)

此評估項在 `evaluate_piece_coordination` 函數中實現。
*   **雙主教 (Bishop Pair)**: 獎勵擁有兩個或更多主教的一方。殘局獎勵（20）高於中局（10），反映了雙主教在開放局面下的威力。
*   **開放線/半開放線上的車 (Rook on Open/Semi-Open File)**: 獎勵佔據了無己方兵（半開放線）或雙方都無兵（開放線）檔案的車。開放線的獎勵更高。
*   **正確性**: **正確**。所有邏輯和獎勵值設定都符合公認的西洋棋戰略原則。

---

## 綜合與動態評估項

### 6. 主動權獎勵 (Initiative Bonus)

*   **作用**: 給予當前執棋方一個 `+10` 的微小獎勵，承認「輪到誰走」本身就是一種優勢。
*   **實現方式**: 此獎勵只在遊戲階段值 `phase` 高於 40% 時生效，在殘局中會自動消失。
*   **正確性**: **正確**。設計簡單且合理。

### 7. 行動力 (Mobility)

*   **作用**: 獎勵擁有更多移動選擇的棋子，懲罰移動受限的棋子。
*   **實現方式**: 在 `evaluate_mobility` 函數中，計算每種棋子（不含兵王）的偽合法移動（pseudo-legal moves）數量。分數由公式 `(實際步數 - 基準步數) * 權重` 得出，獎勵高於平均水平的棋子，懲罰低於平均水平的棋子。
*   **正確性**: **正確**。經過 extensive 的偵錯與驗證，該函數的計算被證實是完全準確的。所有測試中的偏差都源於手動分析未能精確計算所有偽合法移動。

### 8. 王力安全 (King Safety)

這是最複雜的評估模組，在 `evaluate_king_safety` 中實現，由多個子項加權構成。
*   **兵的屏障 (Pawn Shield)**: 評估王前方兵陣的完整性，是王安全最重要的正面因素。
*   **攻擊者 (King Zone Attackers)**: 根據攻擊王周邊區域的敵方棋子數量和類型，計算一個非線性的威脅分數。
*   **王的趨向性 (King Tropism)**: 根據敵方所有棋子到王的距離，計算潛在威脅。
*   **兵風暴 (Pawn Storm)**: 特別懲罰深入到王區域的敵方兵。
*   **動態縮放 (Dynamic Scaling)**: 這是該模組的點睛之筆。它將上述所有因素計算出的「原始威脅分」，乘以一個基於「敵方剩餘攻擊子力總量」的縮放因子（0.0-1.0）。
*   **正確性**: **基本正確，但存在一個潛在的邏輯缺陷**。該模組的整體設計非常精妙，在多數情況下能正確權衡各種因素。然而，在 `_evaluate_king_attackers` 子函數中，存在一個邏輯問題：
    ```python
    temp_occ = all_pieces_occupancy & ~king_zone
    ...
    attacks = get_bishop_attacks(sq, temp_occ & ~BB_SQUARES[sq])
    ```
    這段程式碼在計算滑動棋子（象、車、后）的攻擊時，會從 `occupancy` bitboard 中移除整個 `king_zone`。這會導致一個問題：如果王的身邊有己方棋子作為防守者，這個防守棋子也會被從 `occupancy` 中移除，使得敵方滑動棋子的攻擊可以「穿透」這個防守者，從而錯誤地高估了攻擊的威脅。雖然在我設計的測試案例中，這個 bug 並未導致最終評分出現明顯錯誤，但它是一個潛在的、違反直覺的邏輯缺陷，**建議修正**。

---

## 總結

`evaluation.py` 模組實現了一個現代、全面且功能強大的評估函數。它成功地運用了錐形評估框架，將子力、位置、結構、協同、主動權、機動性和王力安全等多個維度的評估無縫地結合在一起。程式碼的實現高效，邏輯清晰。本次深入分析發現，**除了一個位於王力安全評估中的潛在邏輯缺陷外，其餘部分的程式碼均為正確**。
