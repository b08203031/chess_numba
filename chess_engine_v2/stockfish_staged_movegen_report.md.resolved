# Stockfish 18 Staged Move Generation 深度分析報告

## 一、Stockfish 18 MovePicker 架構解析

### 1.1 核心設計哲學

Stockfish 的 `MovePicker` 類別（位於 `movepick.cpp/h`）是整個搜尋引擎效能的心臟。其設計遵循一個核心原則：

> **「永遠只在必要時才做最少的工作」(Do minimal work, only when needed)**

具體而言，`MovePicker` 透過 `next_move()` 方法每次只吐出一個棋步。搜尋引擎在主迴圈中反覆呼叫 `next_move()` 直到：
1. 發生 Beta Cutoff（找到足夠好的走法），此時剩餘棋步**完全不會被生成**
2. `next_move()` 回傳 `Move::none()`，表示所有棋步都已用盡

### 1.2 階段定義（Stages Enum）

Stockfish 定義了**四套獨立的階段流程**，根據節點類型自動選擇：

```
// 主搜尋 (Main Search)
MAIN_TT → CAPTURE_INIT → GOOD_CAPTURE → QUIET_INIT → GOOD_QUIET → BAD_CAPTURE → BAD_QUIET

// 將殺閃避 (Evasion - 被將軍時)
EVASION_TT → EVASION_INIT → EVASION

// ProbCut
PROBCUT_TT → PROBCUT_INIT → PROBCUT

// 靜態搜尋 (Quiescence Search)
QSEARCH_TT → QCAPTURE_INIT → QCAPTURE
```

### 1.3 主搜尋階段流程詳解

| 階段 | 動作 | 關鍵細節 |
|:-----|:-----|:---------|
| **MAIN_TT** | 直接回傳 TT Move | 不做任何生成，零成本 |
| **CAPTURE_INIT** | 生成所有 Captures，用 MVV + CaptureHistory 打分 | **不呼叫 SEE**，只用 `7 * PieceValue[victim] + captureHistory` |
| **GOOD_CAPTURE** | 逐一取出最佳 Capture，對每個執行 **延遲 SEE** | `see_ge(*cur, -cur->value / 18)`，失敗的移到 `endBadCaptures` |
| **QUIET_INIT** | 生成所有 Quiet Moves，用 History 打分 | 使用 `partial_insertion_sort` 排序，閾值 `-3560 * depth` |
| **GOOD_QUIET** | 逐一取出分數 > `-14000` 的 Quiet | 低於閾值的被視為 Bad Quiet |
| **BAD_CAPTURE** | 回傳之前被 SEE 篩掉的壞吃子步 | 在所有 Good Quiet 之後才嘗試 |
| **BAD_QUIET** | 回傳之前被閾值篩掉的低分 Quiet | 最後的選擇 |

### 1.4 關鍵技術亮點

#### (a) 延遲 SEE（Lazy SEE）
```cpp
// GOOD_CAPTURE 階段
if (pos.see_ge(*cur, -cur->value / 18))
    return true;   // 通過 → 回傳作為好吃子步
std::swap(*endBadCaptures++, *cur);
return false;       // 失敗 → 暫存到壞吃子步
```
- SEE 只在**輪到該步時**才呼叫，而非提前對所有 Captures 計算
- 閾值是動態的：`-cur->value / 18`，分數高的 Capture 門檻更寬鬆

#### (b) Good/Bad Quiet 分流
```cpp
constexpr int goodQuietThreshold = -14000;

// GOOD_QUIET
if (cur->value > goodQuietThreshold) return *(cur - 1);

// BAD_QUIET
return select([&]() { return cur->value <= goodQuietThreshold; });
```
- 把低分的 Quiet 推遲到 Bad Captures 之後再嘗試
- 歷史分數非常差的 Quiet 幾乎永遠不會被搜尋到

#### (c) skip_quiet_moves() 機制
```cpp
void MovePicker::skip_quiet_moves() { skipQuiets = true; }
```
- 搜尋演算法可以在任何時候呼叫此方法**跳過所有剩餘的 Quiet Moves**
- 用於 LMP、Futility Pruning 等場景，連生成都不做

#### (d) 吃子步的計分公式
```cpp
m.value = (*captureHistory)[pc][to][type_of(capturedPiece)]
        + 7 * int(PieceValue[capturedPiece]);
```
- MVV（Most Valuable Victim）乘以 7 倍作為主排序鍵
- CaptureHistory 作為輔助排序，學習哪些吃子在歷史上是好的

#### (e) 安靜步的計分公式
```cpp
m.value = 2 * (*mainHistory)[us][m.raw()];
m.value += 2 * sharedHistory->pawn_entry(pos)[pc][to];
m.value += (*continuationHistory[0])[pc][to]; // 1-ply
m.value += (*continuationHistory[1])[pc][to]; // 2-ply
m.value += (*continuationHistory[2])[pc][to]; // 4-ply
m.value += (*continuationHistory[3])[pc][to]; // 6-ply
m.value += (*continuationHistory[5])[pc][to]; // 更遠

// 給予先導將軍的移動加分
m.value += (bool(pos.check_squares(pt) & to) && pos.see_ge(m, -75)) * 16384;

// 威脅感知：逃離低價子威脅的加分/移向威脅的減分
int v = 20 * (bool(threatByLesser[pt] & from) - bool(threatByLesser[pt] & to));
m.value += PieceValue[pt] * v;

// 淺層歷史
if (ply < LOW_PLY_HISTORY_SIZE)
    m.value += 8 * (*lowPlyHistory)[ply][m.raw()] / (1 + ply);
```

---

## 二、engine_v2 當前架構分析

### 2.1 Move Generation 流程

engine_v2 在 [_search](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#401-1346) 函數中的流程為：

```python
# 第 883 行：一次性生成所有步（captures + quiets 混在一起）
move_count = generate_pseudo_legal_moves_buffer(...)

# 第 912 行：一次性對所有步計分（內含 SEE）
score_moves(...)

# 第 936 行：Selection Sort 迴圈逐一取出
for i in range(move_count):
    # Selection Sort 找最佳
    # Make Move, Legality Check, 搜尋, Unmake
```

### 2.2 score_moves 函數分析

[score_moves](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#L139-L214) 是一個**巨石函數**，對所有棋步執行：

1. **TT Move**：直接給 `SCORE_TT_MOVE (100000)`
2. **Captures**：
   - 呼叫 `see_ge()` 判斷好壞 → **全域 SEE，最大的效能黑洞**
   - 好吃子：`SCORE_GOOD_CAPTURE_BONUS + mvv_lva + capture_history`
   - 壞吃子：`SCORE_BAD_CAPTURE_PENALTY + mvv_lva + capture_history`
3. **Quiets**：
   - Killer moves 優先
   - Counter move
   - `history_table + butterfly_history + continuation_history`

### 2.3 Quiescence Search 分析

QS 同樣呼叫完整的 [score_moves()](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215)（內含 SEE），然後用 Selection Sort 迭代所有 captures。這在戰術激烈的局面下成為嚴重瓶頸。

---

## 三、差異分析表

| 維度 | Stockfish 18 | engine_v2 | 影響 |
|:-----|:-------------|:----------|:-----|
| **生成策略** | 延遲分階段（Captures 與 Quiets 分開生成） | 一次性全生成（[generate_pseudo_legal_moves_buffer](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/move_generator.py#441-451)） | 80-90% 節點在前 1-3 步就 Cutoff，剩餘步白白浪費 CPU |
| **SEE 呼叫時機** | 僅在 GOOD_CAPTURE 階段逐一呼叫 | 在 [score_moves](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215) 中對所有 Captures 批次呼叫 | SEE 是大量計算，提前計算是嚴重浪費 |
| **SEE 閾值** | 動態閾值 `-cur->value / 18` | 固定閾值 `0` | Stockfish 更精確判斷 good/bad capture |
| **Quiet 計分時機** | QUIET_INIT 階段才計分 | 與 Captures 一起在節點最前端計分 | 當 TT/Captures 即 Cutoff 時，Quiet 的計分完全浪費 |
| **Good/Bad Quiet 分流** | 有，閾值 `-14000` | 無 | Stockfish 確保低質量 Quiet 排在 Bad Captures 之後 |
| **skip_quiet_moves** | 有，搜尋可主動跳過所有 Quiet | 無 | LMP等剪枝後仍需遍歷所有 Quiet |
| **Evasion 專用路徑** | 有，被將軍時使用 EVASION 流程 | 無，使用通用生成器 | Stockfish 在被將軍時的生成和排序更高效 |
| **ProbCut 專用路徑** | 有，內建 SEE 閾值篩選 | 另外呼叫 [score_moves](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215) | 多餘的計算 |
| **QSearch 路徑** | 有，極簡的 QSEARCH_TT → QCAPTURE | 呼叫完整 [score_moves](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215)（含 SEE） | QS 中的 SEE 佔比極高 |
| **Capture 計分** | `7 * PieceValue + captureHistory`（不含 SEE） | `mvv_lva + capture_history + see_ge()` | Stockfish 計分極快 |
| **Quiet 計分深度** | 6 層 Continuation History + threatByLesser | 1 層 Continuation History | engine_v2 排序信號較弱 |

---

## 四、engine_v2 五大核心不足

### 不足 1：全域 SEE 預計算（Critical）

**問題**：[score_moves](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215) 對每個 Capture 都呼叫 `see_ge()`。SEE 需要動態更新 Bitboard 並尋找最小攻擊者，是非常昂貴的函數。

**Stockfish 做法**：CAPTURE_INIT 只用 `7 * PieceValue + captureHistory` 打分（純查表，O(1)）。SEE 延遲到 GOOD_CAPTURE 階段，只在該步真要被搜尋時才呼叫。

**影響**：假設一個節點有 8 個 Captures，其中第一個就造成 Beta Cutoff，engine_v2 白白計算了 7 個 SEE，Stockfish 只計算了 1 個。

### 不足 2：缺乏分階段生成（Critical）

**問題**：[generate_pseudo_legal_moves_buffer](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/move_generator.py#441-451) 一次生成所有步（30-50 步），包含 Captures 和 Quiets。

**Stockfish 做法**：先只生成 Captures。只有當 TT Move 和所有好 Captures 都無法 Cutoff 時，才生成 Quiets。

**影響**：大量節點在 TT Move 或 1-2 個 Capture 後即被剪斷。這些節點中，Quiet 步的生成完全是浪費。

### 不足 3：缺乏 Good/Bad Quiet 分流（Important）

**問題**：engine_v2 的 Quiet 步按 history score 排序後依序搜尋，沒有區分好壞。

**Stockfish 做法**：Good Quiet（score > -14000）排在 Bad Captures 之前。Bad Quiet（score ≤ -14000）排在所有其他步之後。

**影響**：一些歷史分數很差的 Quiet 步被排在一些尚可的 Bad Captures 前面，導致搜尋效率降低。

### 不足 4：QSearch 計分過重（Important）

**問題**：QSearch 呼叫完整的 [score_moves()](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/chess_engine_v2/chess_engine/search.py#139-215)，內含 SEE、History、Continuation History 等。

**Stockfish 做法**：QSearch 只生成 Captures，用 MVV + CaptureHistory 計分，不呼叫 SEE。

**影響**：QSearch 佔搜尋量的 60-80%。這裡的每一次多餘計算都被放大。

### 不足 5：缺乏 skip_quiet_moves 機制（Minor）

**問題**：LMP 觸發後，engine_v2 仍需遍歷剩餘的所有 Quiet（用 `continue` 跳過）。

**Stockfish 做法**：`skip_quiet_moves()` 一鍵跳過所有 Quiet 步的生成和遍歷。

**影響**：相對較小，但在深度搜尋中累積效應不可忽視。

---

## 五、優化建議優先級

| 優先級 | 優化項目 | 預期 NPS 提升 | 風險 |
|:------|:---------|:-------------|:-----|
| **P0** | 延遲 SEE（GOOD_CAPTURE 階段逐步呼叫） | **15-30%** | 低 |
| **P0** | 分階段生成（Captures → Quiets） | **10-20%** | 中（需新增 quiets_buffer） |
| **P1** | QSearch 輕量化計分（移除 SEE） | **5-15%** | 低 |
| **P1** | Good/Bad Quiet 分流 | **3-8%** | 低 |
| **P2** | skip_quiet_moves 機制 | **1-3%** | 低 |
| **P2** | Evasion 專用路徑 | **<1%** | 中 |
