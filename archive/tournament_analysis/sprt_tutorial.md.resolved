# 瓦爾德序貫機率比檢定 (Wald's SPRT) 統計學原理解析

在評估西洋棋引擎（或其他演算法）的強弱時，傳統上我們需要對戰固定數量的對局（例如 1000 局），然後再對勝率進行評估。然而，當兩個引擎的實力懸殊，或者實力完全一致時，我們可能在前 100 局就已經可以看出結果了，繼續測試到 1000 局只會浪費寶貴的計算資源。

為了解決這個問題，統計學家亞伯拉罕·瓦爾德 (Abraham Wald) 在二戰期間發明了**序貫機率比檢定**（Sequential Probability Ratio Test, 簡稱 **SPRT**）。這是一套「**一邊做實驗，一邊做決定**」的強大統計工具。

本節將像教科書一樣，由淺入深為您推導 SPRT 的核心邏輯、決策邊界的計算（包含 2.94 的由來），以及它在您的 [tournament.py](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/tournament.py) 代碼中究竟代表什麼數學意義。

---

## 1. 核心概念：兩種假設與機率型錯誤

任何假設檢定（Hypothesis Testing）的出發點，都是要分辨兩種互斥的情境：

*   **虛無假設 (Null Hypothesis, $H_0$)**：新舊引擎強度一樣（實質進步 $\Delta Elo = 0$）。
*   **對立假設 (Alternative Hypothesis, $H_1$)**：新引擎真的有變強（例如進步 $\Delta Elo = 10$）。

作為人類（或測試腳本），我們希望做出正確的判斷，於是我們必須先設定「我們可以容忍犯錯的機率」——也就是**統計決策理論中的兩種錯誤**：

1.  **第一型錯誤 (Type I Error, 符號：$\alpha$)**：**假陽性 (False Positive)**
    *   **定義**：$H_0$ 是對的（引擎沒變強），但你卻判定 $H_1$ 是對的（宣稱它變強了）。
    *   **後果**：把一個爛代碼合（Merge）進了主分支。
    *   **設定值**：通常我們設定 $\alpha = 0.05$ (5%)，表示我們最多只接受 5% 的情況下看走眼。
2.  **第二型錯誤 (Type II Error, 符號：$\beta$)**：**假陰性 (False Negative)**
    *   **定義**：$H_1$ 是對的（引擎真的變強了），但你卻判定 $H_0$ 是對的（說它沒進步而丟棄代碼）。
    *   **後果**：丟失了一個實質的改進。
    *   **設定值**：與 $\alpha$ 相同，通常我們也設定 $\beta = 0.05$ (5%)。

> 在您的代碼中 [SPRTTest(elo0=0.0, elo1=10.0, alpha=0.05, beta=0.05)](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/tournament.py#117-159) 正是定義了這四個參數。

---

## 2. 邊界的數學推導：為什麼是 2.94？

SPRT 最美妙的地方在於，它的決策邊界（Boundaries）是**完全由你設定的容錯率 ($\alpha, \beta$) 推導出來的，與你的測試項目（棋局、醫學測試）毫無關聯。**

根據 Wald 的理論，我們設置兩個邊界：**接受 $H_1$ 的上限 $A$**，與**拒絕 $H_1$（接受 $H_0$）的下限 $B$**。其公式如下：

*   **上限 ($A$)** = $\frac{(\text{你在 } H_1 \text{ 為真時做出正確決定的機率})}{(\text{你在 } H_0 \text{ 為真時做出錯誤決定的機率})}$ = $\frac{1 - \beta}{\alpha}$
*   **下限 ($B$)** = $\frac{(\text{你在 } H_1 \text{ 為真時做出錯誤決定的機率})}{(\text{你在 } H_0 \text{ 為真時做出正確決定的機率})}$ = $\frac{\beta}{1 - \alpha}$

現在，把我們設定的 $\alpha = 0.05, \beta = 0.05$ 代入公式中：

1.  $A = \frac{1 - 0.05}{0.05} = \frac{0.95}{0.05} = 19$
2.  $B = \frac{0.05}{1 - 0.05} = \frac{0.05}{0.95} = \frac{1}{19}$

### LLR 的誕生 (對數轉換)
機率都是一些碎小的數字，如果連續相乘會導致小數點嚴重下溢（Underflow）。在數學上，最常見的做法就是**取自然對數 ($\ln$)，將連乘變成連加**：

*   **對數上限** $\ln(A) = \ln(19) \approx 2.9444389$
*   **對數下限** $\ln(B) = \ln(1 / 19) = -\ln(19) \approx -2.9444389$

**這就是 2.94 的由來！它代表一種「強度」。**
> 簡單來說，當比值（Likelihood Ratio）達到 **19 倍** 時，取自然對數就是 **2.94**。這告訴我們：「當**這組比賽結果在 $H_1$ 宇宙發生的機率，是在 $H_0$ 宇宙發生的機率的 19 倍時**，我們有足夠充分的證據（95% 的信心）宣告：新引擎進步了！」

---

## 3. 似然函數 (Likelihood) 是什麼？如何轉換每局勝負？

我們知道了邊界是 2.94，但我們怎麼計算出目前的累積分數（LLR, Log-Likelihood Ratio）呢？

回到前面剛才講的兩個平行宇宙：
*   **宇宙 $H_0$ (沒進步)**：新舊引擎等強 ($\Delta Elo = 0$)。
*   **宇宙 $H_1$ (有進步)**：新引擎比舊引擎強 ($\Delta Elo = +10$)。

首先，西洋棋界有一套標準的 **Elo 機率羅吉斯模型**將 Elo 分差轉換成預期勝率 (Expected Score)：
$$P(Win) = \frac{1}{1 + 10^{\frac{- \Delta Elo}{400}}}$$

1.  在 $H_0$ 宇宙下，預期勝率是 $\mu_0 = \frac{1}{1 + 10^{\frac{-0}{400}}} = 0.50000$ (50%)
2.  在 $H_1$ 宇宙下，預期勝率是 $\mu_1 = \frac{1}{1 + 10^{\frac{-10}{400}}} \approx 0.51435$ (51.4%)

### 每一局棋的機率比
假設今天新引擎下了一局，結果是**贏 (得分 = 1)**：
*   在 $H_1$ 宇宙發生這件事的機率是 $0.51435$。
*   在 $H_0$ 宇宙發生這件事的機率是 $0.50000$。
*   單局的機率比 = $\frac{0.51435}{0.50000} \approx 1.0287$ （這個比值大於 1，代表證據倒向 $H_1$）
*   單局的對數似然比 (LLR) = $\ln(1.0287) \approx +0.0283$ 

假設今天結果是**輸 (得分 = 0)**：
*   在 $H_1$ 宇宙的機率是 $(1 - 0.51435) = 0.48565$
*   在 $H_0$ 宇宙的機率是 $(1 - 0.50000) = 0.50000$
*   單局的機率比 = $\frac{0.48565}{0.50000} \approx 0.9713$ （比值小於 1，代表證據倒向 $H_0$）
*   單局的對數似然比 (LLR) = $\ln(0.9713) \approx -0.0291$

**只要下很多盤棋，把每一盤的單局 LLR 通通加起來，只要這條線撞到了 $+2.94$ 或 $-2.94$，檢定就立刻結束。**

---

## 4. [tournament.py](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/tournament.py) 裡的常態分佈近似算法 (Normal Approximation)

在現實的引擎測試中，西洋棋有「和棋 (Draws)」，更有「白棋先手優勢」。為了消除先後手優勢，我們會讓引擎兩兩一組，針對相同開局下兩盤棋（黑白互換），這也是為什麼您的腳本裡叫做 `Pair Scores`。

當樣本數變大的時候，我們不需要去算每一局精確的查表機率，我們可以使用統計學中的大魔王——**中央極限定理 (Central Limit Theorem, CLT)**。

只要將對戰結果的分數 $S$ 取平均、算變異數，我們就可以把這些亂七八糟的分數逼近成一個「常態分佈的鐘形曲線」。這也是您在 [check_status()](file:///c:/Users/ren%20cian/OneDrive/%E6%A1%8C%E9%9D%A2/chess/chess_numba/chess_numba/tournament.py#128-159) 看到的那段落長公式的原理：

$$ LLR \approx \frac{\mu_1 - \mu_0}{\sigma^2} \times \left( \sum_{i=1}^n S_i - n \times \frac{\mu_0 + \mu_1}{2} \right) $$

**逐項解析你的代碼：**
*   **$\mu_0$ 與 $\mu_1$**：這就是 $H_0$ 和 $H_1$ 時空下的預期得分（代碼第 141-146 行：一對棋局就是把單局的勝率乘以 2）。
*   **$\sigma^2$ (variance)**：這是觀測分數的變異數 (Variance)。和棋會讓變異數很小，輸贏互見會讓變異數變大。**「和棋越多，變異數越小，除以極小的數字後，LLR 就會劇烈跳動，我們就能越快地得出誰比較強的結論。」**這也就是為什麼現代引擎測試通常和棋率高達 80%~90%，SPRT卻只用極短的時間就能分出勝負的秘密。
*   **最後的大刮號**：它在計算「實際總得分 $\sum S_i$」與「臨界中線（$\frac{\mu_0 + \mu_1}{2}$）」的差。如果實際得分比 $0.507$（也就是 0.500 和 0.514 的一半）還要高，那這個乘積就會一直往正數飛升；如果比 $0.507$ 低，就會往負數下墜。

---

## 5. 課堂總結：如何看懂終端機結果

下次您如果看到：
```
SPRT LLR: -1.38 bounds: [-2.94, 2.94]
SPRT Status: Continue
```

你可以像統計學大師一樣解讀這件事：
1.  這是一個為了有 95% 信心說「新舊是否有 10 Elo 差距」的統計測試。
2.  要達到 95% 的信心，你的觀測機率（Likelihood Ratio）要相差 19 倍（$\pm 2.94$）。
3.  目前 LLR 是 $-1.38$，如果用自然指數還原它：$e^{-1.38} \approx 0.25$ $\Rightarrow \frac{1}{4}$。
4.  這代表：**這些比賽結果證明「新引擎沒退步、且比舊版強 10 Elo 以上」的機率，居然只有「新版比舊版弱」的 $\frac{1}{4}$！**（此為假定測試方為舊版時的講法。此時，數據正在告訴你，v2把v1打爆了，H0 的可能性遠低於被擊倒的可能性。）
5.  因為還沒頂到 19 倍 / $\frac{1}{19}$ (2.94 / -2.94)，統計測試認為樣本還稍微嫌不夠，於是說「繼續測 (Continue)」。但勝負的趨勢實際上已經確定了。

以上就是完整的原理，它完美兼顧了「節約資源 (提早停損)」與「嚴格容錯」，是現在資料科學家必備的思維武器！
