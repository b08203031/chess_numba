# SEE 剪枝與犧牲戰術分析 (SEE Pruning Analysis)

## 1. SEE 剪枝原理 (Principle)

SEE (Static Exchange Evaluation) 剪枝主要應用於 **Quiescence Search (QS)** 和 **Main Search** 中，其核心目的是**快速剔除明顯虧損的吃子移動**，從而減少無效搜尋節點，提升搜尋效率。

*   **機制**：在搜尋一個吃子移動前，先呼叫 `see(move)` 計算該移動的靜態交換值。
*   **條件**：如果 `see_value < SEE_THRESHOLD`（例如 -100 分），則直接剪掉該分支，不進行遞歸搜尋。
*   **邏輯**：如果一個吃子在靜態交換下虧損超過一個兵（例如用后吃兵被兵吃回），那麼在絕大多數情況下，這是一個壞棋。

## 2. 是否會影響犧牲戰術？ (Does it hurt Sacrifices?)

這是一個非常關鍵的問題。犧牲戰術（Sacrifices）本質上就是「靜態虧損，動態獲利」。如果 SEE 剪枝過於激進，確實可能剪掉天才般的棄子戰術。

### 2.1 為什麼通常**不會**造成問題？

1.  **閾值寬容度 (Threshold Margin)**：
    *   目前的設定是 `SEE_THRESHOLD = -100`（相當於虧損一個兵）。
    *   這意味著「輕微虧損」的棄子（例如棄半子換取攻勢，SEE 可能為 -50）**不會**被剪枝。
    *   只有「嚴重虧損」（如送后、送車）才會被考慮剪枝。

2.  **不剪將軍 (No Pruning on Check)**：
    *   在 `quiescence_search` 中，如果當前局面**處於被將軍狀態**，或者是**給出將軍的移動**（視實作而定，通常 QS 只搜吃子，但 Main Search 會搜將軍），通常會禁用剪枝或放寬條件。
    *   我的實作中，QS 內部的 SEE 剪枝條件通常會加上 `if not is_check` 保護。

3.  **主搜尋深度 (Main Search Depth)**：
    *   SEE 剪枝主要在 **Quiescence Search**（靜態搜尋）中最強力。
    *   QS 的目的是「平息」局面，處理明顯的吃子。複雜的棄子戰術通常發生在 Main Search 的深層節點。
    *   在 Main Search 中，SEE 通常用於 **Move Ordering**（排序），將壞吃子排在後面搜，而不是直接剪掉（除非是特定的 Pruning 如 SEE Pruning at shallow depth）。
    *   **關鍵點**：即使 SEE 認為是壞棋（排在後面），只要搜尋深度足夠，它最終還是會被搜尋到。只有在極端剪枝（如 LMP, Futility）結合 SEE 時才可能完全忽略。

### 2.2 潛在風險與解決方案

*   **風險**：如果是「棄后殺王」（Queen Sacrifice for Mate），SEE 值會非常低（例如 -900）。如果 QS 啟用了 SEE 剪枝且閾值是 -100，這個移動在 QS 中會被直接剪掉。
*   **現實**：
    *   如果棄后直接導致將殺，Main Search 通常會延伸搜尋。
    *   如果棄后只是為了破壞結構，而後續沒有立即的強制手段，QS 剪枝確實可能錯過。
    *   **但是**，QS 的設計本意就是處理「無需思考的吃子」。棄后通常需要「思考」（Main Search）。如果棄后是為了長遠利益，它應該在 Main Search 中被評估，而不是依賴 QS 的 Horizon。

## 3. 結論

*   **原理**：SEE 剪枝利用靜態評估過濾掉「顯然」的壞吃子。
*   **戰術影響**：對於大多數標準棄子（Sacrifices），由於搜尋深度和排序機制，SEE 不會造成漏算。
*   **極端情況**：對於 QS 階段的深遠棄子，確實存在被剪枝的風險，這是搜尋效率與精度的權衡（Trade-off）。Stockfish 通過精細的閾值調整（例如 `threshold = -200 * depth`）來動態平衡這一點。
*   **目前引擎**：我們使用了相對保守的 `-100` 閾值，這在過濾顯著 blunder 的同時，保留了大部分合理的戰術可能性。

總結來說，SEE 剪枝是現代引擎的標配，只要參數得當，它對棋力的**提升（效率）遠大於風險（漏算）**。
